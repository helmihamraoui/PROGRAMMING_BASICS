var min_age =10 ;
var min_height=42;
function valide (min_age , min_height){
    if ( height < min_height && age < min_age) {
        return "the conditions are not availabe";
    }
     else (height>= min_height && age >= min_age) 
      {    return "the rider can go on the ride ";}
    
}